-r requirements.txt
-r requirements_test_pre_commit.txt

# The compat shim (openccu_loom_client.compat.aiohomematic.central) bridges
# events onto the real aiohomematic event bus, so its unit tests need the
# real package installed.
coverage==7.16.0
mypy==2.3.1
prek==0.5.0
pur==7.4.0
pytest==9.1.1
pytest-aiohttp==1.1.1
pytest-asyncio==1.4.0
pytest-cov==7.1.0
pylint==4.0.7
# tests/unit/test_operations_contract.py reads the daemon's assets/openapi.yaml
# to check every URL operations/ builds against the documented contract.
pyyaml==6.0.3
